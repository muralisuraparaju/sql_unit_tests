-- Databricks notebook source
-- MAGIC %sql
-- MAGIC CREATE OR REPLACE TEMP VIEW MONTHLY_AVG_TEMPARATURE_BY_ELEVATION AS 
-- MAGIC SELECT MONTH, ELEVATION_GRADE, ROUND( AVG(AVG_TEMP_F), 1) AS AVG_TEMP FROM STATION_DATA
-- MAGIC GROUP BY MONTH, ELEVATION_GRADE ORDER BY MONTH, ELEVATION_GRADE