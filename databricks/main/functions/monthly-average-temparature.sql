-- Databricks notebook source
-- MAGIC %sql
-- MAGIC CREATE OR REPLACE TEMP VIEW MONTHLY_AVG_TEMPARATURE AS 
-- MAGIC SELECT MONTH, ROUND( AVG(AVG_TEMP_C), 1) AS AVG_TEMP FROM STATION_DATA
-- MAGIC GROUP BY MONTH ORDER BY MONTH