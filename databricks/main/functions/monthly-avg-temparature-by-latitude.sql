-- Databricks notebook source
-- MAGIC %sql
-- MAGIC CREATE OR REPLACE TEMP VIEW MONTHLY_AVG_TEMPARATURE_BY_LATITUDE AS
-- MAGIC SELECT MONTH, LATITUDE_ZONE, ROUND( AVG(AVG_TEMP_F), 1) AS AVG_TEMP FROM STATION_DATA
-- MAGIC GROUP BY MONTH, LATITUDE_ZONE ORDER BY MONTH, LATITUDE_ZONE